# Cargo configuration, see: https://doc.rust-lang.org/cargo/reference/config.html
# Schema configured in ../tombi.toml

alias.lint = "clippy --workspace --all-targets --all-features -- -D warnings -D clippy::all"
